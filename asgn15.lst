1]
INSERT INTO MULTICUST (SELECT * FROM SALESPEOPLE);

2]
DELETE FROM CUSTOMER WHERE CNUM NOT IN (SELECT CNUM FROM ORDERS);

3]
UPDATE SALESPEOPLE SET COMM = COMM * 1.2 WHERE SNUM IN (SELECT SNUM FROM ORDERS GROUP BY SNUM HAVING SUM(AMT) > 3000);
